## CSS Colours
## ===========
##
## This module is autogenerated with a list of CSS colours.

from ../css/cssTypes import CssColor

proc `$`*(color: CssColor): string =
    ## Stringifies `CssColor`
    result = color.repr

proc newCssColor*[T](repr: T): CssColor =
    ## Creates new `CssColor`
    result = CssColor(repr: $repr)

proc newCssColour*[T](repr: T): CssColor =
    ## Creates new `CssColor`
    result = newCssColor(repr)

const
    AliceBlue*: CssColor = newCssColor("AliceBlue") ## #f0f8ff
    AntiqueWhite*: CssColor = newCssColor("AntiqueWhite") ## #faebd7
    Aqua*: CssColor = newCssColor("Aqua") ## #00ffff
    Aquamarine*: CssColor = newCssColor("Aquamarine") ## #7fffd4
    Azure*: CssColor = newCssColor("Azure") ## #f0ffff
    Beige*: CssColor = newCssColor("Beige") ## #f5f5dc
    Bisque*: CssColor = newCssColor("Bisque") ## #ffe4c4
    Black*: CssColor = newCssColor("Black") ## #000000
    BlanchedAlmond*: CssColor = newCssColor("BlanchedAlmond") ## #ffebcd
    Blue*: CssColor = newCssColor("Blue") ## #0000ff
    BlueViolet*: CssColor = newCssColor("BlueViolet") ## #8a2be2
    Brown*: CssColor = newCssColor("Brown") ## #a52a2a
    BurlyWood*: CssColor = newCssColor("BurlyWood") ## #deb887
    CadetBlue*: CssColor = newCssColor("CadetBlue") ## #5f9ea0
    Chartreuse*: CssColor = newCssColor("Chartreuse") ## #7fff00
    Chocolate*: CssColor = newCssColor("Chocolate") ## #d2691e
    Coral*: CssColor = newCssColor("Coral") ## #ff7f50
    CornflowerBlue*: CssColor = newCssColor("CornflowerBlue") ## #6495ed
    CornSilk*: CssColor = newCssColor("CornSilk") ## #fff8dc
    Crimson*: CssColor = newCssColor("Crimson") ## #dc143c
    Cyan*: CssColor = newCssColor("Cyan") ## #00ffff (synonym of aqua)
    DarkbBlue*: CssColor = newCssColor("DarkbBlue") ## #00008b
    DarkCyan*: CssColor = newCssColor("DarkCyan") ## #008b8b
    DarkGoldenRod*: CssColor = newCssColor("DarkGoldenRod") ## #b8860b
    DarkGray*: CssColor = newCssColor("DarkGray") ## #a9a9a9
    DarkGreen*: CssColor = newCssColor("DarkGreen") ## #006400
    DarkGrey*: CssColor = newCssColor("DarkGrey") ## #a9a9a9
    DarkKhaki*: CssColor = newCssColor("DarkKhaki") ## #bdb76b
    DarkMagenta*: CssColor = newCssColor("DarkMagenta") ## #8b008b
    DarkOliveGreen*: CssColor = newCssColor("DarkOliveGreen") ## #556b2f
    DarkOrange*: CssColor = newCssColor("DarkOrange") ## #ff8c00
    DarkOrchid*: CssColor = newCssColor("DarkOrchid") ## #9932cc
    DarkRed*: CssColor = newCssColor("DarkRed") ## #8b0000
    DarkSalmon*: CssColor = newCssColor("DarkSalmon") ## #e9967a
    DarkSeaGreen*: CssColor = newCssColor("DarkSeaGreen") ## #8fbc8f
    DarkSlateBlue*: CssColor = newCssColor("DarkSlateBlue") ## #483d8b
    DarkSlateGray*: CssColor = newCssColor("DarkSlateGray") ## #2f4f4f
    DarkSlateGrey*: CssColor = newCssColor("DarkSlateGrey") ## #2f4f4f
    DarkTurquoise*: CssColor = newCssColor("DarkTurquoise") ## #00ced1
    DarkViolet*: CssColor = newCssColor("DarkViolet") ## #9400d3
    DeepPink*: CssColor = newCssColor("DeepPink") ## #ff1493
    DeepSkyBlue*: CssColor = newCssColor("DeepSkyBlue") ## #00bfff
    DimGray*: CssColor = newCssColor("DimGray") ## #696969
    DimGrey*: CssColor = newCssColor("DimGrey") ## #696969
    DodgerBlue*: CssColor = newCssColor("DodgerBlue") ## #1e90ff
    DireBrick*: CssColor = newCssColor("DireBrick") ## #b22222
    FloralWhite*: CssColor = newCssColor("FloralWhite") ## #fffaf0
    ForestGreen*: CssColor = newCssColor("ForestGreen") ## #228b22
    Fuchsia*: CssColor = newCssColor("Fuchsia") ## #ff00ff
    Gainsboro*: CssColor = newCssColor("Gainsboro") ## #dcdcdc
    GhostWhite*: CssColor = newCssColor("GhostWhite") ## #f8f8ff
    Gold*: CssColor = newCssColor("Gold") ## #ffd700
    GoldenRod*: CssColor = newCssColor("GoldenRod") ## #daa520
    Gray*: CssColor = newCssColor("Gray") ## #808080
    Green*: CssColor = newCssColor("Green") ## #008000
    GreenYellow*: CssColor = newCssColor("GreenYellow") ## #adff2f
    Grey*: CssColor = newCssColor("Grey") ## #808080 (synonym of gray)
    Honeydew*: CssColor = newCssColor("Honeydew") ## #f0fff0
    HotPink*: CssColor = newCssColor("HotPink") ## #ff69b4
    IndianRed*: CssColor = newCssColor("IndianRed") ## #cd5c5c
    Indigo*: CssColor = newCssColor("Indigo") ## #4b0082
    Ivory*: CssColor = newCssColor("Ivory") ## #fffff0
    Khaki*: CssColor = newCssColor("Khaki") ## #f0e68c
    Lavender*: CssColor = newCssColor("Lavender") ## #e6e6fa
    LavenderBlush*: CssColor = newCssColor("LavenderBlush") ## #fff0f5
    LawnGreen*: CssColor = newCssColor("LawnGreen") ## #7cfc00
    LemonChiffon*: CssColor = newCssColor("LemonChiffon") ## #fffacd
    LightBlue*: CssColor = newCssColor("LightBlue") ## #add8e6
    LightCoral*: CssColor = newCssColor("LightCoral") ## #f08080
    LightCyan*: CssColor = newCssColor("LightCyan") ## #e0ffff
    LightGoldenRodYellow*: CssColor = newCssColor("LightGoldenRodYellow") ## #fafad2
    LightGray*: CssColor = newCssColor("LightGray") ## #d3d3d3
    LightGreen*: CssColor = newCssColor("LightGreen") ## #90ee90
    LightGrey*: CssColor = newCssColor("LightGrey") ## #d3d3d3
    LightPink*: CssColor = newCssColor("LightPink") ## #ffb6c1
    LightSalmon*: CssColor = newCssColor("LightSalmon") ## #ffa07a
    LightSeaGreen*: CssColor = newCssColor("LightSeaGreen") ## #20b2aa
    LightSkyBlue*: CssColor = newCssColor("LightSkyBlue") ## #87cefa
    LightSlateGray*: CssColor = newCssColor("LightSlateGray") ## #778899
    LightSlateGrey*: CssColor = newCssColor("LightSlateGrey") ## #778899
    LightSteelBlue*: CssColor = newCssColor("LightSteelBlue") ## #b0c4de
    LightYellow*: CssColor = newCssColor("LightYellow") ## #ffffe0
    Lime*: CssColor = newCssColor("Lime") ## #00ff00
    LimeGreen*: CssColor = newCssColor("LimeGreen") ## #32cd32
    Linen*: CssColor = newCssColor("Linen") ## #faf0e6
    Magenta*: CssColor = newCssColor("Magenta") ## #ff00ff (synonym of fuchsia)
    Maroon*: CssColor = newCssColor("Maroon") ## #800000
    MediumAquamarine*: CssColor = newCssColor("MediumAquamarine") ## #66cdaa
    MediumBlue*: CssColor = newCssColor("MediumBlue") ## #0000cd
    MediumOrchid*: CssColor = newCssColor("MediumOrchid") ## #ba55d3
    MediumPurple*: CssColor = newCssColor("MediumPurple") ## #9370db
    MediumSeaGreen*: CssColor = newCssColor("MediumSeaGreen") ## #3cb371
    MediumSlateBlue*: CssColor = newCssColor("MediumSlateBlue") ## #7b68ee
    MediumSpringGreen*: CssColor = newCssColor("MediumSpringGreen") ## #00fa9a
    MediumTurquoise*: CssColor = newCssColor("MediumTurquoise") ## #48d1cc
    MediumVioletRed*: CssColor = newCssColor("MediumVioletRed") ## #c71585
    MidnightBlue*: CssColor = newCssColor("MidnightBlue") ## #191970
    MintCream*: CssColor = newCssColor("MintCream") ## #f5fffa
    MistyRose*: CssColor = newCssColor("MistyRose") ## #ffe4e1
    Moccasin*: CssColor = newCssColor("Moccasin") ## #ffe4b5
    NavajoWhite*: CssColor = newCssColor("NavajoWhite") ## #ffdead
    Navy*: CssColor = newCssColor("Navy") ## #000080
    OldLace*: CssColor = newCssColor("OldLace") ## #fdf5e6
    Olive*: CssColor = newCssColor("Olive") ## #808000
    OliveDrab*: CssColor = newCssColor("OliveDrab") ## #6b8e23
    Orange*: CssColor = newCssColor("Orange") ## #ffa500
    OrangeRed*: CssColor = newCssColor("OrangeRed") ## #ff4500
    Orchid*: CssColor = newCssColor("Orchid") ## #da70d6
    PaleGoldenRod*: CssColor = newCssColor("PaleGoldenRod") ## #eee8aa
    PaleGreen*: CssColor = newCssColor("PaleGreen") ## #98fb98
    PaleTurquoise*: CssColor = newCssColor("PaleTurquoise") ## #afeeee
    PaleVioletRed*: CssColor = newCssColor("PaleVioletRed") ## #db7093
    PapayaWhip*: CssColor = newCssColor("PapayaWhip") ## #ffefd5
    PeachPuff*: CssColor = newCssColor("PeachPuff") ## #ffdab9
    Peru*: CssColor = newCssColor("Peru") ## #cd853f
    Pink*: CssColor = newCssColor("Pink") ## #ffc0cb
    Plum*: CssColor = newCssColor("Plum") ## #dda0dd
    PowderBlue*: CssColor = newCssColor("PowderBlue") ## #b0e0e6
    Purple*: CssColor = newCssColor("Purple") ## #800080
    RebeccaPurple*: CssColor = newCssColor("RebeccaPurple") ## #663399
    Red*: CssColor = newCssColor("Red") ## #ff0000
    RosyBrown*: CssColor = newCssColor("RosyBrown") ## #bc8f8f
    RoyalBlue*: CssColor = newCssColor("RoyalBlue") ## #4169e1
    SaddleBrown*: CssColor = newCssColor("SaddleBrown") ## #8b4513
    Salmon*: CssColor = newCssColor("Salmon") ## #fa8072
    SandyBrown*: CssColor = newCssColor("SandyBrown") ## #f4a460
    SeaGreen*: CssColor = newCssColor("SeaGreen") ## #2e8b57
    SeaShell*: CssColor = newCssColor("SeaShell") ## #fff5ee
    Sienna*: CssColor = newCssColor("Sienna") ## #a0522d
    Silver*: CssColor = newCssColor("Silver") ## #c0c0c0
    SkyBlue*: CssColor = newCssColor("SkyBlue") ## #87ceeb
    SlateBlue*: CssColor = newCssColor("SlateBlue") ## #6a5acd
    SlateGray*: CssColor = newCssColor("SlateGray") ## #708090
    SlateGrey*: CssColor = newCssColor("SlateGrey") ## #708090
    Snow*: CssColor = newCssColor("Snow") ## #fffafa
    SpringGreen*: CssColor = newCssColor("SpringGreen") ## #00ff7f
    SteelBlue*: CssColor = newCssColor("SteelBlue") ## #4682b4
    Tan*: CssColor = newCssColor("Tan") ## #d2b48c
    Teal*: CssColor = newCssColor("Teal") ## #008080
    Thistle*: CssColor = newCssColor("Thistle") ## #d8bfd8
    Tomato*: CssColor = newCssColor("Tomato") ## #ff6347
    Transparent*: CssColor = newCssColor("Transparent") ## See transparent.
    Turquoise*: CssColor = newCssColor("Turquoise") ## #40e0d0
    Violet*: CssColor = newCssColor("Violet") ## #ee82ee
    Wheat*: CssColor = newCssColor("Wheat") ## #f5deb3
    White*: CssColor = newCssColor("White") ## #ffffff
    Whitesmoke*: CssColor = newCssColor("Whitesmoke") ## #f5f5f5
    Yellow*: CssColor = newCssColor("Yellow") ## #ffff00
    Yellowgreen*: CssColor = newCssColor("Yellowgreen") ## #9acd32
    AccentColor*: CssColor = newCssColor("AccentColor") ## Background of accented user interface controls.
    AccentColorText*: CssColor = newCssColor("AccentColorText") ## Text of accented user interface controls.
    ActiveText*: CssColor = newCssColor("ActiveText") ## Text of active links.
    ButtonBorder*: CssColor = newCssColor("ButtonBorder") ## Base border color of controls.
    ButtonFace*: CssColor = newCssColor("ButtonFace") ## Background color of controls.
    ButtonText*: CssColor = newCssColor("ButtonText") ## Text color of controls.
    Canvas*: CssColor = newCssColor("Canvas") ## Background of application content or documents.
    CanvasText*: CssColor = newCssColor("CanvasText") ## Text color in application content or documents.
    Field*: CssColor = newCssColor("Field") ## Background of input fields.
    FieldText*: CssColor = newCssColor("FieldText") ## Text in input fields.
    GrayText*: CssColor = newCssColor("GrayText") ## Text color for disabled items (for example, a disabled control).
    Highlight*: CssColor = newCssColor("Highlight") ## Background of selected items.
    HighlightText*: CssColor = newCssColor("HighlightText") ## Text color of selected items.
    LinkText*: CssColor = newCssColor("LinkText") ## Text of non-active, non-visited links.
    Mark*: CssColor = newCssColor("Mark") ## Background of text that has been specially marked (such as by the HTML mark element).
    MarkText*: CssColor = newCssColor("MarkText") ## Text that has been specially marked (such as by the HTML mark element).
    SelectedItem*: CssColor = newCssColor("SelectedItem") ## Background of selected items, for example, a selected checkbox.
    SelectedItemText*: CssColor = newCssColor("SelectedItemText") ## Text of selected items.
    VisitedText*: CssColor = newCssColor("VisitedText") ## Text of visited links.
    ActiveBorder*: CssColor = newCssColor("ActiveBorder") ## Active window border
    ActiveCaption*: CssColor = newCssColor("ActiveCaption") ## Active window caption. Should be used with CaptionText as foreground color.
    AppWorkspace*: CssColor = newCssColor("AppWorkspace") ## Background color of multiple document interface.
    Background*: CssColor = newCssColor("Background") ## Desktop background.
    ButtonHighlight*: CssColor = newCssColor("ButtonHighlight") ## The color of the border facing the light source for 3-D elements that appear 3-D due to that layer of surrounding border.
    ButtonShadow*: CssColor = newCssColor("ButtonShadow") ## The color of the border away from the light source for 3-D elements that appear 3-D due to that layer of surrounding border.
    CaptionText*: CssColor = newCssColor("CaptionText") ## Text in caption, size box, and scrollbar arrow box. Should be used with the ActiveCaption background color.
    InactiveBorder*: CssColor = newCssColor("InactiveBorder") ## Inactive window border.
    InactiveCaption*: CssColor = newCssColor("InactiveCaption") ## Inactive window caption. Should be used with the InactiveCaptionText foreground color.
    InactiveCaptionText*: CssColor = newCssColor("InactiveCaptionText") ## Color of text in an inactive caption. Should be used with the InactiveCaption background color.
    InfoBackground*: CssColor = newCssColor("InfoBackground") ## Background color for tooltip controls. Should be used with the InfoText foreground color.
    InfoText*: CssColor = newCssColor("InfoText") ## Text color for tooltip controls. Should be used with the InfoBackground background color.
    Menu*: CssColor = newCssColor("Menu") ## Menu background. Should be used with the MenuText or -moz-MenuBarText foreground color.
    MenuText*: CssColor = newCssColor("MenuText") ## Text in menus. Should be used with the Menu background color.
    Scrollbar*: CssColor = newCssColor("Scrollbar") ## Background color of scroll bars.
    ThreeDDarkShadow*: CssColor = newCssColor("ThreeDDarkShadow") ## The color of the darker (generally outer) of the two borders away from the light source for 3-D elements that appear 3-D due to two concentric layers of surrounding border.
    ThreeDFace*: CssColor = newCssColor("ThreeDFace") ## The face background color for 3-D elements that appear 3-D due to two concentric layers of surrounding border. Should be used with the ButtonText foreground color.
    ThreeDHighlight*: CssColor = newCssColor("ThreeDHighlight") ## The color of the lighter (generally outer) of the two borders facing the light source for 3-D elements that appear 3-D due to two concentric layers of surrounding border.
    ThreeDLightShadow*: CssColor = newCssColor("ThreeDLightShadow") ## The color of the darker (generally inner) of the two borders facing the light source for 3-D elements that appear 3-D due to two concentric layers of surrounding border.
    ThreeDShadow*: CssColor = newCssColor("ThreeDShadow") ## The color of the lighter (generally inner) of the two borders away from the light source for 3-D elements that appear 3-D due to two concentric layers of surrounding border.
    Window*: CssColor = newCssColor("Window") ## Window background. Should be used with the WindowText foreground color.
    WindowFrame*: CssColor = newCssColor("WindowFrame") ## Window frame.
    WindowText*: CssColor = newCssColor("WindowText") ## Text in windows. Should be used with the Window background color.
